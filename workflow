init work on branch: 
	git pull origin main- resolve conflit if any
	
finish work:
	on branch: git pull origin main - resolve conflit if any
	git add .
	git commit -m "message"
	git push
	gcm (git check main)
	git pull - conflit if any (atualizate main)
	git merge yourbranch
	git push
	gco yourbranch





Eu poderia desde o inicio colocar cada coisa no seu lugar
e a partir daí realizar as conversoes necessárias.

Se eu criar um array void posso com type cast ir colocando
tudo que eu precisar lá dentro e apartir daí faço as conversões.

